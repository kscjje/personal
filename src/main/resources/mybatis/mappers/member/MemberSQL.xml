<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Member">

    <!-- 회원정보::목록조회  -->
    <select id="selectMemberList" parameterType="com.custom.v1.member.service.MemberVO" resultType="egovMap">
    	SELECT 
    		  MEM_NO
    		, MEM_NM
    		, USER_ID
    	FROM MEMBER
        
    </select>
    
</mapper>